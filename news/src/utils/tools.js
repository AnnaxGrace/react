
//select top five elements in the data array.
export const selectTopFive = data => {
    const resultsArray = [];
    for (let i = 0; i < 5; i++) {
        resultsArray.push(data)
    }
    return resultsArray
}

